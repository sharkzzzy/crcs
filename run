# 运行这个找路径
from huggingface_hub import snapshot_download

try:
    path = snapshot_download(repo_id="stabilityai/stable-diffusion-xl-base-1.0", local_files_only=True)
    print(f"模型在：{path}")
except:
    print("本地确实找不到，看来真的得下载...")
